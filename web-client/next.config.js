/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // We removed the rewrites because /api/auth is now handled internally by Next.js
}

module.exports = nextConfig
